<!--pages/question/question.wxml-->
<view class="page">
    <view bindtap="goLook" data-id="{{item.id}}" data-kind="{{item.kind}}" class="question-list" wx:for="{{list}}"
        wx:key="index">
        {{index+1}}. {{item.title}}
    </view>
</view>